#include <stdio.h>

int main() {
  // *:ARITHMETIC OPERATORS -----> + - * / % ++ --

  // ?: + (addition)
  // ?: - (subtraction)
  // ?: * (multiplication)
  // ?: / (division)
  // ?: % (modulus)
  // ?: ++ (increment by 1)
  // ?: -- (decrement by 1)

  int x = 5;
  int y = 2;

  // int z = x + y; output = 7
  // int z = x - y; output = 3
  // int z = x * y; output = 10
  // int z = x / y; output = 2
  // float z = x / (float)y; output = 2.50000
  // int z = x % y; output = 1

  printf("%d", z);

  return 0;
}